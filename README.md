# Desktop-File-Sorter
Simple Python file sorter from one directory to another.
Just define your source directory and target directory at the top (I like to sort from my downloads to a different folder on my desktop).

To Use:
enter directory and use command, nohup python3 cleandesk.py & (after defining file paths) to run it as a background process
